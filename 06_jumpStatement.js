console.log(`===================continue statement======================`);
for (let index = 0; index <= 10; index++) {
    console.log(`iteration start`);
    
    if (index == 5) {
        
        // continue;
        break;
    }

        console.log(`the value is ${index} and intertion is ${index + 1}`);
        console.log(`End of iteration`);
        
        
    
}   